{
  "name": "blog-django-react",
  "version": "1.0.1",
  "description": "Blogging platform",
  "main": "index.js",
  "proxy": "http://localhost:8000",
  "license": "MIT",
  "private": true,
  "scripts": {
    "heroku-prebuild": "NODE_ENV=production cd blog_frontend/ && yarn install && yarn build && cd ..",
    "start": "yarn start"
  },
  "cacheDirectories": [
    "blog_frontend/node_modules"
  ],
  "engines": {
    "node": "16.4.1",
    "npm": "7.18.1"
  }
}
